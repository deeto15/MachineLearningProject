services:
  redis:
    image: redis:latest
    container_name: redis
    ports:
      - "6379:6379"
    volumes:
      - redis_data:/data

  scraper:
    build: ./scraper
    container_name: scraper
    env_file: 'scraper/.env'
    depends_on:
      - redis
  
volumes:
  redis_data: